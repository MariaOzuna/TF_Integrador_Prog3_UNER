Proyecto: BREAD Turnos - Victoria Avalos

Instrucciones:
1. Copiar .env.example a .env y modificar variables (DB + email)
2. npm install
3. npm start

Rutas:
GET /api/turnos
POST /api/turnos
PUT /api/turnos/:id
DELETE /api/turnos/:id
